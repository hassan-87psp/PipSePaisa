PipSePaisa V241 — Trading Sessions Indicator + Final Sidebar Sorting

WHAT THIS PATCH DOES
1. Fixes the client sidebar sorting at the actual root cause: the final flex-order lock.
   Final position is now:
   Signals
   EA & Indicator
   Live Charts
2. Keeps only one EA & Indicator nav item and normalizes its position.
3. Adds a new published product:
   PipSePaisa Trading Sessions Indicator — MT5
   - 100% Free
   - MT5
   - Version 1.0
   - Private account-linked licensed file
4. Adds the supplied indicator screenshot as the product cover/gallery image.
5. Adds session-specific product-page copy so the page does not show Pivot-only wording.

DEPLOYMENT
A. Upload/replace these website files/folders preserving paths:
   index.html
   eaindicator/index.html
   ea-indicator-user-v215.js
   user-app-core-v29.js
   assets/ea-indicator/trading-sessions-indicator-mt5.png

B. Supabase SQL Editor:
   Run 103_V241_TRADING_SESSIONS_INDICATOR.sql once.
   It is safe to run again if needed.

C. Hard refresh desktop (Ctrl+F5). On mobile fully close and reopen the browser/PWA.

NO EDGE FUNCTION REDEPLOY IS REQUIRED.
NO PAYMENT, COURSE, AUTH OR ADMIN PAYMENT LOGIC IS CHANGED.
