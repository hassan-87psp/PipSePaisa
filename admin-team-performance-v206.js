/* PipSePaisa V206.4 — Team Performance & Earnings Management (Admin) */
(function(){
'use strict';
if(window.__pspAdminTeamPerformanceV206)return;window.__pspAdminTeamPerformanceV206=true;
const SUPABASE_URL='https://etfolhinohgmskbfjoyh.supabase.co';
const SUPABASE_KEY='sb_publishable_LgmfuH2ePiY8fxNGs7nTTA_FSS_oPBw';
let fallbackClient=null,overview=[],links=[],dailyRows=[],adminClients=[],activeTab='overview',loading=false;
const $=id=>document.getElementById(id);
const esc=v=>String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const num=v=>Number(v||0);
const fmt=v=>num(v).toLocaleString(undefined,{maximumFractionDigits:2});
const money=v=>'$'+num(v).toLocaleString(undefined,{minimumFractionDigits:0,maximumFractionDigits:2});
function getSb(){try{if(typeof sb!=='undefined'&&sb)return sb;if(window.sb)return window.sb;if(window.adminSb)return window.adminSb;if(!fallbackClient&&window.supabase?.createClient)fallbackClient=window.supabase.createClient(SUPABASE_URL,SUPABASE_KEY,{auth:{storageKey:'pipsepaisa-admin-auth-v2',persistSession:true,autoRefreshToken:true}});return fallbackClient;}catch(_){return null;}}
async function waitSb(){for(let i=0;i<50;i++){const c=getSb();if(c?.rpc&&c?.from)return c;await new Promise(r=>setTimeout(r,100));}return null;}
function toast(m,t='success'){if(window.pipToast)window.pipToast(m,t);else if(window.showToast)window.showToast(m,t);else console.log(m);}
function period(){const y=Number($('tpYear')?.value||new Date().getFullYear()),m=Number($('tpMonth')?.value||new Date().getMonth()+1);return `${y}-${String(m).padStart(2,'0')}-01`;}
function todayMY(){try{const p=Object.fromEntries(new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kuala_Lumpur',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date()).map(x=>[x.type,x.value]));return `${p.year}-${p.month}-${p.day}`;}catch(_){return new Date().toISOString().slice(0,10);}}
function months(){return ['January','February','March','April','May','June','July','August','September','October','November','December'];}
function metricRow(r){return r?.metrics||{};}
function addStyles(){if($('tpV206Styles'))return;const s=document.createElement('style');s.id='tpV206Styles';s.textContent=`
#page-teamperformance{--tp-gold:#FB9201;--tp-gold2:#f5c56d;--tp-ink:#0c1320}.tp-shell{position:relative;isolation:isolate}.tp-shell:before{content:'';position:absolute;right:4%;top:-50px;width:280px;height:280px;border-radius:50%;background:radial-gradient(circle,rgba(251,146,1,.13),transparent 68%);filter:blur(4px);z-index:-1;pointer-events:none}.tp-hero{display:flex;align-items:flex-end;justify-content:space-between;gap:16px;padding:21px;border:1px solid var(--border);border-radius:18px;background:linear-gradient(135deg,var(--bg-card),var(--bg-elevated));box-shadow:var(--shadow-sm);margin-bottom:14px;overflow:hidden;position:relative}.tp-hero:after{content:'';position:absolute;width:180px;height:180px;border:1px solid rgba(251,146,1,.18);border-radius:50%;right:7%;top:-105px;box-shadow:0 0 0 28px rgba(251,146,1,.04)}.tp-eyebrow{font-size:8px;letter-spacing:.12em;text-transform:uppercase;color:var(--tp-gold);font-weight:950}.tp-hero h2{margin:5px 0 4px;font-size:20px;color:var(--text-primary);font-weight:950}.tp-hero p{margin:0;color:var(--text-muted);font-size:9px;line-height:1.5;max-width:760px}.tp-period{display:flex;gap:7px;position:relative;z-index:1}.tp-period select,.tp-filter input,.tp-filter select,.tp-editor input,.tp-editor select,.tp-editor textarea{border:1px solid var(--border);background:var(--bg-elevated);color:var(--text-primary);border-radius:9px;padding:8px 10px;font:inherit;font-size:9px;outline:none}.tp-period select:focus,.tp-filter input:focus,.tp-filter select:focus,.tp-editor input:focus,.tp-editor select:focus,.tp-editor textarea:focus{border-color:var(--tp-gold);box-shadow:0 0 0 3px rgba(251,146,1,.08)}
.tp-tabs{display:flex;gap:6px;align-items:center;overflow:auto;padding:5px;border:1px solid var(--border);border-radius:13px;background:var(--bg-card);margin-bottom:14px}.tp-tab{white-space:nowrap;border:0;background:transparent;color:var(--text-muted);font:inherit;font-size:8px;font-weight:900;padding:9px 11px;border-radius:9px;cursor:pointer}.tp-tab:hover{color:var(--text-primary);background:var(--bg-elevated)}.tp-tab.active{background:linear-gradient(135deg,#FB9201,#f7bd5c);color:#111827;box-shadow:0 7px 16px rgba(251,146,1,.18)}
.tp-kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-bottom:14px}.tp-kpi{border:1px solid var(--border);border-radius:15px;background:var(--bg-card);padding:14px;min-width:0;position:relative;overflow:hidden}.tp-kpi:before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,var(--tp-gold),transparent)}.tp-kpi span{font-size:7px;text-transform:uppercase;letter-spacing:.08em;color:var(--text-muted);font-weight:900}.tp-kpi strong{display:block;font-size:21px;color:var(--text-primary);margin-top:6px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.tp-kpi small{display:block;font-size:7px;color:var(--text-muted);margin-top:4px}.tp-panel{border:1px solid var(--border);border-radius:16px;background:var(--bg-card);padding:14px;margin-bottom:14px;box-shadow:var(--shadow-sm)}.tp-head{display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap;margin-bottom:10px}.tp-head h3{font-size:13px;margin:0;color:var(--text-primary);font-weight:950}.tp-head p{margin:3px 0 0;color:var(--text-muted);font-size:8px}.tp-filter{display:flex;gap:6px;flex-wrap:wrap}.tp-filter input{min-width:210px}.tp-tablewrap{overflow:auto;border:1px solid var(--border);border-radius:12px}.tp-table{width:100%;border-collapse:collapse;min-width:1140px;background:var(--bg-elevated)}.tp-table th{padding:10px;text-align:left;font-size:7px;text-transform:uppercase;letter-spacing:.07em;color:var(--text-muted);background:rgba(251,146,1,.06);white-space:nowrap}.tp-table td{padding:10px;border-top:1px solid var(--border);font-size:8px;color:var(--text-primary);vertical-align:middle}.tp-name{font-size:9px;font-weight:950}.tp-sub{font-size:7px;color:var(--text-muted);margin-top:3px}.tp-badge{display:inline-flex;align-items:center;padding:4px 7px;border-radius:999px;font-size:7px;font-weight:900;border:1px solid rgba(251,146,1,.22);background:rgba(251,146,1,.08);color:#d97800}.tp-badge.auto{border-color:rgba(16,185,129,.25);background:rgba(16,185,129,.08);color:#059669}.tp-badge.override{border-color:rgba(139,92,246,.25);background:rgba(139,92,246,.08);color:#7c3aed}.tp-btn{border:1px solid var(--border);background:var(--bg-elevated);color:var(--text-primary);border-radius:8px;padding:7px 9px;font:inherit;font-size:8px;font-weight:900;cursor:pointer}.tp-btn:hover{border-color:var(--tp-gold);color:#d97800}.tp-btn.primary{background:#FB9201;border-color:#FB9201;color:#111827}.tp-btn.danger{color:#dc2626}.tp-empty{text-align:center!important;padding:28px!important;color:var(--text-muted)!important}.tp-grid2{display:grid;grid-template-columns:1fr 1fr;gap:12px}.tp-editor{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px}.tp-editor .wide{grid-column:1/-1}.tp-editor label{display:block;color:var(--text-muted);font-size:7px;font-weight:900;text-transform:uppercase;letter-spacing:.06em;margin:0 0 4px}.tp-editor input,.tp-editor select,.tp-editor textarea{width:100%;box-sizing:border-box}.tp-editor textarea{min-height:65px;resize:vertical}.tp-editor-help{font-size:7px;color:var(--text-muted);line-height:1.45;margin-top:4px}.tp-auto-box{border:1px solid var(--border);border-radius:12px;padding:12px;background:var(--bg-elevated)}.tp-auto-box strong{display:block;font-size:18px;color:var(--text-primary);margin-top:4px}.tp-auto-box span{font-size:7px;color:var(--text-muted);text-transform:uppercase;font-weight:900}.tp-impact{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.tp-modal{position:fixed;inset:0;z-index:9000;display:none;place-items:center;background:rgba(4,8,16,.7);backdrop-filter:blur(6px);padding:16px}.tp-modal.open{display:grid}.tp-modal-card{width:min(700px,100%);max-height:90vh;overflow:auto;border:1px solid rgba(251,146,1,.28);border-radius:18px;background:var(--bg-card);box-shadow:0 35px 100px rgba(0,0,0,.4)}.tp-modal-head{display:flex;align-items:center;justify-content:space-between;padding:14px 16px;border-bottom:1px solid var(--border)}.tp-modal-head h3{margin:0;font-size:14px}.tp-modal-body{padding:15px}.tp-notice{padding:10px 11px;border:1px solid rgba(251,146,1,.2);background:rgba(251,146,1,.06);border-radius:10px;color:var(--text-muted);font-size:8px;line-height:1.5}.tp-daily-manual{font-weight:900}.tp-daily-auto{color:#d97800;font-weight:900}.tp-history-bars{display:flex;align-items:end;gap:7px;height:150px;padding:12px 5px 0}.tp-history-bar{flex:1;min-width:25px;display:flex;flex-direction:column;align-items:center;justify-content:end;height:100%;gap:5px}.tp-history-bar i{display:block;width:100%;max-width:38px;min-height:3px;border-radius:6px 6px 2px 2px;background:linear-gradient(180deg,#f6bb55,#FB9201);box-shadow:0 5px 12px rgba(251,146,1,.14)}.tp-history-bar span{font-size:6px;color:var(--text-muted)}
@media(max-width:1150px){.tp-kpis{grid-template-columns:repeat(2,minmax(0,1fr))}.tp-editor{grid-template-columns:repeat(2,minmax(0,1fr))}.tp-grid2{grid-template-columns:1fr}.tp-impact{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:650px){.tp-hero{align-items:flex-start;flex-direction:column}.tp-period{width:100%}.tp-period select{flex:1}.tp-kpis{grid-template-columns:repeat(2,minmax(0,1fr))}.tp-editor{grid-template-columns:1fr}.tp-filter input{min-width:0;width:100%}.tp-impact{grid-template-columns:1fr}}
`;document.head.appendChild(s);}
function addMenuPage(){if(document.querySelector('[data-page="teamperformance"]'))return;const nav=document.querySelector('nav.menu');if(nav){const anchor=document.querySelector('.menu-item[data-page="teamaccess"]')||document.querySelector('.menu-item[data-page="logs"]');const item=document.createElement('div');item.className='menu-item';item.dataset.page='teamperformance';item.innerHTML='<span class="menu-icon">🏆</span>Team Performance';item.onclick=()=>window.showPage('teamperformance',item);if(anchor)anchor.parentNode.insertBefore(item,anchor);else nav.appendChild(item);}const content=$('content');if(!content)return;const page=document.createElement('div');page.className='page';page.id='page-teamperformance';page.innerHTML=`<div class="tp-shell">
<div class="tp-hero"><div><div class="tp-eyebrow">Team Earnings Command Center</div><h2>Team Performance</h2><p>Monthly registrations, clients, course/VIP commissions, broker lots, earnings, tracking attribution and daily activity in one workspace.</p></div><div class="tp-period"><select id="tpMonth"></select><select id="tpYear"></select><button class="tp-btn" id="tpRefresh">↻ Refresh</button></div></div>
<div class="tp-tabs" id="tpTabs"><button class="tp-tab active" data-tptab="overview">Overview</button><button class="tp-tab" data-tptab="members">Team Members</button><button class="tp-tab" data-tptab="editor">Performance Editor</button><button class="tp-tab" data-tptab="daily">Daily Reports</button><button class="tp-tab" data-tptab="clients">Clients</button><button class="tp-tab" data-tptab="links">Tracking Links</button><button class="tp-tab" data-tptab="history">Performance History</button></div>
<div id="tpBody"></div></div>
<div class="tp-modal" id="tpLinkModal"><div class="tp-modal-card"><div class="tp-modal-head"><h3>Edit Tracking Link</h3><button class="tp-btn" id="tpLinkClose">✕</button></div><div class="tp-modal-body" id="tpLinkModalBody"></div></div></div>
<div class="tp-modal" id="tpLotsModal"><div class="tp-modal-card"><div class="tp-modal-head"><h3>Daily Report Correction</h3><button class="tp-btn" id="tpLotsClose">✕</button></div><div class="tp-modal-body" id="tpLotsModalBody"></div></div></div>`;content.appendChild(page);fillPeriod();bind();}
function fillPeriod(){const m=$('tpMonth'),y=$('tpYear');if(!m||!y)return;const n=new Date();m.innerHTML=months().map((x,i)=>`<option value="${i+1}" ${i===n.getMonth()?'selected':''}>${x}</option>`).join('');const yy=n.getFullYear();y.innerHTML=Array.from({length:6},(_,i)=>yy-4+i).map(v=>`<option ${v===yy?'selected':''}>${v}</option>`).join('');}
function bind(){$('tpMonth').onchange=load;$('tpYear').onchange=load;$('tpRefresh').onclick=load;$('tpTabs').onclick=e=>{const b=e.target.closest('[data-tptab]');if(!b)return;activeTab=b.dataset.tptab;document.querySelectorAll('#tpTabs .tp-tab').forEach(x=>x.classList.toggle('active',x===b));renderTab();};$('tpLinkClose').onclick=()=>$('tpLinkModal').classList.remove('open');$('tpLotsClose').onclick=()=>$('tpLotsModal').classList.remove('open');$('tpLinkModal').onclick=e=>{if(e.target===$('tpLinkModal'))$('tpLinkModal').classList.remove('open')};$('tpLotsModal').onclick=e=>{if(e.target===$('tpLotsModal'))$('tpLotsModal').classList.remove('open')};}
async function load(){if(loading)return;loading=true;try{const c=await waitSb();if(!c)throw new Error('Supabase not ready');let o=await c.rpc('psp_admin_team_overview_v207',{p_period_month:period()});if(o.error){console.warn('V207 admin overview fallback:',o.error);o=await c.rpc('psp_admin_team_overview_v206',{p_period_month:period()});}const l=await c.from('tracked_links').select('id,name,slug,destination_path,source,campaign,is_active,assigned_team_member_id,created_at').order('created_at',{ascending:false});if(o.error)throw o.error;if(l.error)throw l.error;overview=o.data||[];links=l.data||[];renderTab();}catch(e){$('tpBody').innerHTML=`<div class="tp-panel"><div class="tp-empty"><strong>Team Performance V206 is not ready.</strong><div class="tp-sub" style="margin-top:7px">Run the latest V206.4 SQL in Supabase. ${esc(e.message||'')}</div></div></div>`;}finally{loading=false;}}
function teamLinks(id){return links.filter(x=>String(x.assigned_team_member_id||'')===String(id));}
function totals(){return overview.reduce((a,r)=>{const m=metricRow(r);a.reg+=num(m.registrations);a.clients+=num(m.clients);a.course+=num(m.course_performance);a.vip+=num(m.vip_clients);a.lots+=num(m.overall_lots);a.earn+=num(m.total_earnings);a.conv+=num(m.conversion_rate);return a;},{reg:0,clients:0,course:0,vip:0,lots:0,earn:0,conv:0});}
function kpis(){const t=totals(),avg=overview.length?t.conv/overview.length:0;return `<div class="tp-kpis"><div class="tp-kpi"><span>Total Team Members</span><strong>${fmt(overview.length)}</strong><small>${overview.filter(x=>x.is_active).length} active</small></div><div class="tp-kpi"><span>Total Registrations</span><strong>${fmt(t.reg)}</strong><small>Selected month</small></div><div class="tp-kpi"><span>Total Clients</span><strong>${fmt(t.clients)}</strong><small>Attributed conversions</small></div><div class="tp-kpi"><span>Course Performance</span><strong>${money(t.course)}</strong><small>Paid commissionable value</small></div><div class="tp-kpi"><span>VIP Clients</span><strong>${fmt(t.vip)}</strong><small>Auto + Admin override</small></div><div class="tp-kpi"><span>Overall Lots</span><strong>${fmt(t.lots)}</strong><small>XM + DPrime + Exness</small></div><div class="tp-kpi"><span>Team Earnings</span><strong>${money(t.earn)}</strong><small>Course + VIP + lots</small></div><div class="tp-kpi"><span>Avg Conversion</span><strong>${fmt(avg)}%</strong><small>Registration → client</small></div></div>`;}
function memberTable(list=overview){return `<div class="tp-tablewrap"><table class="tp-table"><thead><tr><th>Team Member</th><th>Assigned Link</th><th>Registrations</th><th>Clients</th><th>Course Performance</th><th>VIP</th><th>XM Lots</th><th>DPrime</th><th>Exness</th><th>Total Lots</th><th>Total Earnings</th><th>Course Level</th><th>VIP Level</th><th>Lot Level</th><th>Actions</th></tr></thead><tbody>${list.length?list.map(r=>{const m=metricRow(r),ll=teamLinks(r.team_member_id);return `<tr><td><div class="tp-name">${esc(r.display_name||r.username)}</div><div class="tp-sub">@${esc(r.username||'')} · ${r.is_active?'Active':'Disabled'}</div></td><td><div class="tp-name">${esc(ll[0]?.name||'No link')}</div><div class="tp-sub">${ll.length?ll.length+' assigned link'+(ll.length>1?'s':''):'—'}</div></td><td>${fmt(m.registrations)}</td><td>${fmt(m.clients)}</td><td><strong>${money(m.course_performance)}</strong><div class="tp-sub">${fmt(m.course_paid_users)} paid users · <span class="tp-badge ${m.course_override?'override':'auto'}">${m.course_override?'Admin Override':'Auto Calculated'}</span></div></td><td>${fmt(m.vip_clients)}</td><td>${fmt(m.xm_lots)}</td><td>${fmt(m.dprime_lots)}</td><td>${fmt(m.exness_lots)}</td><td><strong>${fmt(m.overall_lots)}</strong></td><td><strong>${money(m.total_earnings)}</strong></td><td>${esc(m.course_level||'—')} · ${fmt(m.course_rate)}% · ${fmt(m.course_paid_users)} users</td><td>${esc(m.vip_level||'—')} · ${fmt(m.vip_rate)}%</td><td>${esc(m.lot_level||'—')}</td><td><button class="tp-btn" onclick="window.TPV206.openEditor('${esc(r.team_member_id)}')">Edit Performance</button> <button class="tp-btn" onclick="window.TPV206.openClients('${esc(r.team_member_id)}')">Clients</button></td></tr>`}).join(''):`<tr><td colspan="15" class="tp-empty">No team members.</td></tr>`}</tbody></table></div>`;}
function renderOverview(){const top=[...overview].sort((a,b)=>num(metricRow(b).total_earnings)-num(metricRow(a).total_earnings)).slice(0,5);$('tpBody').innerHTML=kpis()+`<div class="tp-grid2"><div class="tp-panel"><div class="tp-head"><div><h3>Top Team Performance</h3><p>Highest total earnings for selected month.</p></div></div>${memberTable(top)}</div><div class="tp-panel"><div class="tp-head"><div><h3>Earnings Distribution</h3><p>Quick team comparison.</p></div></div><div class="tp-history-bars">${top.length?top.map(r=>{const m=metricRow(r),mx=Math.max(...top.map(x=>num(metricRow(x).total_earnings)),1),h=Math.max(4,num(m.total_earnings)/mx*120);return `<div class="tp-history-bar" title="${esc(r.display_name)} ${money(m.total_earnings)}"><i style="height:${h}px"></i><span>${esc((r.display_name||'').split(' ')[0])}</span></div>`}).join(''):'<div class="tp-empty">No earnings yet.</div>'}</div></div></div>`;}
function renderMembers(){const html=`${kpis()}<div class="tp-panel"><div class="tp-head"><div><h3>Team Member Performance</h3><p>View monthly marketing, conversions, lots, tiers and earnings.</p></div><div class="tp-filter"><button class="tp-btn" id="tpManageAccounts">Manage Team Accounts</button><input id="tpMemberSearch" placeholder="Search team member…"><select id="tpSourceFilter"><option value="">All Sources</option>${[...new Set(links.map(x=>x.source).filter(Boolean))].map(x=>`<option>${esc(x)}</option>`).join('')}</select><select id="tpCampaignFilter"><option value="">All Campaigns</option>${[...new Set(links.map(x=>x.campaign).filter(Boolean))].map(x=>`<option>${esc(x)}</option>`).join('')}</select></div></div><div id="tpMemberTable">${memberTable()}</div></div>`;$('tpBody').innerHTML=html;$('tpManageAccounts').onclick=()=>{const el=document.querySelector('[data-page="teamaccess"]');if(el&&window.showPage)window.showPage('teamaccess',el);else toast('Team Panel Access page is not available.','error')};['tpMemberSearch','tpSourceFilter','tpCampaignFilter'].forEach(id=>$(id).addEventListener(id==='tpMemberSearch'?'input':'change',filterMembers));}
function filterMembers(){const q=($('tpMemberSearch').value||'').toLowerCase(),s=$('tpSourceFilter').value,c=$('tpCampaignFilter').value;const f=overview.filter(r=>{const ll=teamLinks(r.team_member_id);return (!q||`${r.display_name} ${r.username}`.toLowerCase().includes(q))&&(!s||ll.some(x=>x.source===s))&&(!c||ll.some(x=>x.campaign===c));});$('tpMemberTable').innerHTML=memberTable(f);}
function teamOptions(sel=''){return overview.map(r=>`<option value="${esc(r.team_member_id)}" ${String(sel)===String(r.team_member_id)?'selected':''}>${esc(r.display_name||r.username)}</option>`).join('');}
function renderEditor(selected){const id=selected||overview[0]?.team_member_id,m=metricRow(overview.find(x=>String(x.team_member_id)===String(id))||{});$('tpBody').innerHTML=`<div class="tp-panel"><div class="tp-head"><div><h3>Performance Editor</h3><p>Course/VIP values are automatic by default. Course rate is based on paid users. Broker lots are updated weekly from Daily Reports.</p></div></div><div class="tp-editor"><div class="wide"><label>Team Member</label><select id="tpEditTeam">${teamOptions(id)}</select></div><div class="tp-auto-box"><span>Paid Course Users</span><strong>${fmt(m.course_paid_users)}</strong><div class="tp-editor-help">This user count decides the Course commission tier.</div></div><div class="tp-auto-box"><span>Approved Course Sales</span><strong>${money(m.course_performance_auto)}</strong><div class="tp-editor-help">Commission % is applied to approved paid sales value.</div></div><div class="tp-auto-box"><span>Current Total Earnings</span><strong>${money(m.total_earnings)}</strong><div class="tp-editor-help">Recalculates automatically after changes.</div></div><div><label>Course Sales Value Override</label><input id="tpCourseOverride" type="number" min="0" step="0.01" placeholder="Leave blank = Auto" value="${m.course_override?num(m.course_performance):''}"><div class="tp-editor-help">Changes revenue value only; tier still follows paid user count.</div></div><div><label>VIP Count Override</label><input id="tpVipOverride" type="number" min="0" step="1" placeholder="Leave blank = Auto" value="${m.vip_override?num(m.vip_clients):''}"></div><div><label>VIP Value Override ($)</label><input id="tpVipValue" type="number" min="0" step="0.01" placeholder="Blank = VIP × $50" value="${m.vip_value_is_override?num(m.vip_value_override_value):''}"></div><div class="wide tp-notice"><b>Weekly Lots:</b> Go to <b>Daily Reports</b>, choose a date in the required week, then click <b>Weekly Lots</b> for that Team Member. Monthly earnings automatically add all weekly entries.</div><div class="wide"><label>Admin Note</label><textarea id="tpAdminNote" placeholder="Optional internal note">${esc(m.admin_note||'')}</textarea></div><div class="wide" style="display:flex;justify-content:flex-end;gap:8px"><button class="tp-btn" id="tpResetAuto">Clear Course/VIP Overrides</button><button class="tp-btn primary" id="tpSavePerformance">Save & Recalculate</button></div></div></div><div class="tp-panel"><div class="tp-head"><div><h3>Current Levels</h3><p>Read-only preview for selected month.</p></div></div><div class="tp-impact"><div class="tp-auto-box"><span>Course</span><strong>${fmt(m.course_rate)}%</strong><div class="tp-editor-help">${esc(m.course_level||'—')} · ${fmt(m.course_paid_users)} paid users · ${money(m.course_earnings)}</div></div><div class="tp-auto-box"><span>VIP</span><strong>${fmt(m.vip_rate)}%</strong><div class="tp-editor-help">${esc(m.vip_level||'—')} · ${money(m.vip_earnings)}</div></div><div class="tp-auto-box"><span>XM / DPrime Rate</span><strong>${money(m.xm_dprime_rate)}/lot</strong><div class="tp-editor-help">${esc(m.lot_level||'—')}</div></div><div class="tp-auto-box"><span>Exness Rate</span><strong>${money(m.exness_rate)}/lot</strong><div class="tp-editor-help">Monthly ${fmt(m.overall_lots)} lots</div></div></div></div>`;$('tpEditTeam').onchange=()=>renderEditor($('tpEditTeam').value);$('tpSavePerformance').onclick=savePerformance;$('tpResetAuto').onclick=()=>{$('tpCourseOverride').value='';$('tpVipOverride').value='';$('tpVipValue').value='';};}
function nullableNumber(id){const v=$(id).value.trim();return v===''?null:Number(v);}
async function savePerformance(){try{const c=await waitSb();const id=$('tpEditTeam').value;const args={p_team_member_id:id,p_period_month:period(),p_vip_count_override:nullableNumber('tpVipOverride'),p_vip_value_override:nullableNumber('tpVipValue'),p_course_value_override:nullableNumber('tpCourseOverride'),p_admin_note:$('tpAdminNote').value.trim()||null};$('tpSavePerformance').disabled=true;let r=await c.rpc('psp_admin_save_team_performance_v207',args);if(r.error){console.warn('V207 save fallback:',r.error);const m=metricRow(overview.find(x=>String(x.team_member_id)===String(id))||{});r=await c.rpc('psp_admin_save_team_performance_v206',{...args,p_xm_lots:num(m.xm_lots),p_dprime_lots:num(m.dprime_lots),p_exness_lots:num(m.exness_lots)});}if(r.error)throw r.error;toast('Performance saved and earnings recalculated.');await load();activeTab='editor';document.querySelectorAll('#tpTabs .tp-tab').forEach(x=>x.classList.toggle('active',x.dataset.tptab==='editor'));renderEditor(id);}catch(e){toast(e.message||'Unable to save','error');}finally{if($('tpSavePerformance'))$('tpSavePerformance').disabled=false;}}
function renderDaily(){const d=todayMY();$('tpBody').innerHTML=`<div class="tp-panel"><div class="tp-head"><div><h3>Team Daily Reports</h3><p>Manual outreach/VIP/broker results alongside auto-tracked registrations and course enrollments.</p></div><div class="tp-filter"><input type="date" id="tpDailyDate" value="${d}"><select id="tpDailyTeam"><option value="">All Team Members</option>${teamOptions()}</select><button class="tp-btn" id="tpDailyLoad">Load</button></div></div><div id="tpDailyBody"><div class="tp-empty">Loading…</div></div></div>`;$('tpDailyLoad').onclick=loadDaily;$('tpDailyDate').onchange=loadDaily;$('tpDailyTeam').onchange=loadDaily;loadDaily();}
async function loadDaily(){try{const c=await waitSb();let r=await c.rpc('psp_admin_team_daily_reports_v207',{p_report_date:$('tpDailyDate').value,p_team_member_id:$('tpDailyTeam').value||null});if(r.error){console.warn('V207 daily fallback:',r.error);r=await c.rpc('psp_admin_team_daily_reports_v206',{p_report_date:$('tpDailyDate').value,p_team_member_id:$('tpDailyTeam').value||null});}if(r.error)throw r.error;dailyRows=r.data||[];renderDailyRows();}catch(e){$('tpDailyBody').innerHTML=`<div class="tp-empty">${esc(e.message||'Unable to load daily reports')}</div>`;}}
function renderDailyRows(){const a=dailyRows.reduce((x,r)=>{const m=r.manual||{},z=r.auto||{};x.leads+=num(m.leads);x.msg+=num(m.messages_sent);x.calls+=num(m.calls_made);x.follow+=num(m.follow_ups);x.reg+=num(z.registrations);x.vip+=num(m.vip_converted);x.b2+=num(z.batch2_enrollments);x.fund+=num(z.fundamental_enrollments);x.adv+=num(z.advance_enrollments);x.newacct+=num(m.broker_new_accounts);x.ib+=num(m.ib_shifts);x.lots+=num(m.xm_weekly_lots)+num(m.dprime_weekly_lots)+num(m.exness_weekly_lots);return x;},{leads:0,msg:0,calls:0,follow:0,reg:0,vip:0,b2:0,fund:0,adv:0,newacct:0,ib:0,lots:0});$('tpDailyBody').innerHTML=`<div class="tp-kpis"><div class="tp-kpi"><span>Leads <b>Manual</b></span><strong>${fmt(a.leads)}</strong></div><div class="tp-kpi"><span>Messages <b>Manual</b></span><strong>${fmt(a.msg)}</strong></div><div class="tp-kpi"><span>Registrations <b>Auto</b></span><strong>${fmt(a.reg)}</strong></div><div class="tp-kpi"><span>VIP $50 <b>Manual</b></span><strong>${fmt(a.vip)}</strong></div><div class="tp-kpi"><span>Batch 2 <b>Auto</b></span><strong>${fmt(a.b2)}</strong></div><div class="tp-kpi"><span>Fundamental <b>Auto</b></span><strong>${fmt(a.fund)}</strong></div><div class="tp-kpi"><span>Advance <b>Auto</b></span><strong>${fmt(a.adv)}</strong></div><div class="tp-kpi"><span>New / IB Shift <b>Manual</b></span><strong>${fmt(a.newacct)} / ${fmt(a.ib)}</strong></div></div><div class="tp-tablewrap"><table class="tp-table"><thead><tr><th>Team Member</th><th>Leads</th><th>Messages</th><th>Calls</th><th>Follow-ups</th><th>Registrations</th><th>VIP $50</th><th>Batch 2</th><th>Fundamental</th><th>Advance</th><th>New Account</th><th>IB Shift</th><th>Weekly Lots XM / DP / EX</th><th>Total Lots</th><th>Note</th><th>Admin Correction</th></tr></thead><tbody>${dailyRows.map(r=>{const m=r.manual||{},z=r.auto||{},xl=m.xm_weekly_lots!=null?num(m.xm_weekly_lots):num(z.weekly_xm_lots??z.xm_lots),dl=m.dprime_weekly_lots!=null?num(m.dprime_weekly_lots):num(z.weekly_dprime_lots??z.dprime_lots),el=m.exness_weekly_lots!=null?num(m.exness_weekly_lots):num(z.weekly_exness_lots??z.exness_lots),vip=m.vip_converted!=null?num(m.vip_converted):0,newAcct=m.broker_new_accounts!=null?num(m.broker_new_accounts):0,ib=m.ib_shifts!=null?num(m.ib_shifts):0;return `<tr><td><div class="tp-name">${esc(r.display_name||r.username)}</div><div class="tp-sub">@${esc(r.username||'')}</div></td><td class="tp-daily-manual">${fmt(m.leads)}</td><td class="tp-daily-manual">${fmt(m.messages_sent)}</td><td class="tp-daily-manual">${fmt(m.calls_made)}</td><td class="tp-daily-manual">${fmt(m.follow_ups)}</td><td class="tp-daily-auto">${fmt(z.registrations)}</td><td class="tp-daily-manual"><b>${fmt(vip)}</b></td><td class="tp-daily-auto">${fmt(z.batch2_enrollments)}</td><td class="tp-daily-auto">${fmt(z.fundamental_enrollments)}</td><td class="tp-daily-auto">${fmt(z.advance_enrollments)}</td><td class="tp-daily-manual"><b>${fmt(newAcct)}</b></td><td class="tp-daily-manual"><b>${fmt(ib)}</b></td><td class="tp-daily-manual">${fmt(xl)} / ${fmt(dl)} / ${fmt(el)}<div class="tp-sub">Week ${esc(z.week_start||'—')}</div></td><td><b>${fmt(xl+dl+el)}</b></td><td>${esc(m.note||'—')}</td><td><button class="tp-btn" onclick="window.TPV206.dailyLots('${esc(r.team_member_id)}','${esc(r.display_name||r.username)}')">Correct Lots</button></td></tr>`}).join('')||`<tr><td colspan="16" class="tp-empty">No reports.</td></tr>`}</tbody></table></div>`;}
function renderAdminClients(selected){const id=selected||'';$('tpBody').innerHTML=`<div class="tp-panel"><div class="tp-head"><div><h3>Attributed Clients</h3><p>Persistent tracking ownership connects registrations to later course, VIP and broker conversions.</p></div><div class="tp-filter"><select id="tpClientTeam"><option value="">All Team Members</option>${teamOptions(id)}</select><input id="tpClientSearch" placeholder="Search client, course, broker, source…"><button class="tp-btn" id="tpClientReload">Reload</button></div></div><div id="tpClientBody"><div class="tp-empty">Loading clients…</div></div></div>`;$('tpClientTeam').onchange=loadAdminClients;$('tpClientSearch').oninput=renderAdminClientRows;$('tpClientReload').onclick=loadAdminClients;loadAdminClients();}
async function loadAdminClients(){try{const c=await waitSb();const {data,error}=await c.rpc('psp_admin_team_clients_v206',{p_team_member_id:$('tpClientTeam')?.value||null,p_limit:3000});if(error)throw error;adminClients=data||[];renderAdminClientRows();}catch(e){$('tpClientBody').innerHTML=`<div class="tp-empty">${esc(e.message||'Unable to load clients')}</div>`;}}
function renderAdminClientRows(){if(!$('tpClientBody'))return;const q=($('tpClientSearch')?.value||'').toLowerCase();const rows=adminClients.filter(c=>!q||[c.team_member_name,c.client_id,c.full_name,c.source,c.campaign,c.reference_code,c.course_enrollment,c.vip_status,c.broker,c.account_mode].join(' ').toLowerCase().includes(q));$('tpClientBody').innerHTML=`<div class="tp-tablewrap"><table class="tp-table"><thead><tr><th>Team Member</th><th>Client</th><th>Registration</th><th>Source / Campaign</th><th>Reference</th><th>Course</th><th>VIP</th><th>Broker</th><th>Account Type</th><th>Conversion</th><th>Contact</th></tr></thead><tbody>${rows.map(c=>{const wa=String(c.whatsapp||'').replace(/\D/g,'');return `<tr><td><div class="tp-name">${esc(c.team_member_name||'Team Member')}</div></td><td><div class="tp-name">${esc(c.full_name||'User')}</div><div class="tp-sub">${esc(c.client_id||'—')}</div></td><td>${esc(c.registration_at?new Date(c.registration_at).toLocaleDateString():'—')}</td><td>${esc(c.source||'Other')}<div class="tp-sub">${esc(c.campaign||'No campaign')}</div></td><td>${esc(c.reference_code||'—')}</td><td>${esc(c.course_enrollment||'None')}</td><td>${esc(c.vip_status||'Not VIP')}</td><td>${esc(c.broker||'—')}<div class="tp-sub">${esc(c.broker_status||'—')}</div></td><td>${esc(c.account_mode||'—')}</td><td><span class="tp-badge ${String(c.conversion_status).toLowerCase()==='converted'?'auto':''}">${esc(c.conversion_status||'Registered')}</span></td><td>${wa?`<a class="tp-btn" href="https://wa.me/${wa}" target="_blank" rel="noopener">WhatsApp</a>`:'—'}</td></tr>`}).join('')||`<tr><td colspan="11" class="tp-empty">No attributed clients found.</td></tr>`}</tbody></table></div>`;}
function renderLinks(){const rows=links.filter(x=>x.assigned_team_member_id);$('tpBody').innerHTML=`<div class="tp-panel"><div class="tp-head"><div><h3>Tracking Links</h3><p>Manage assignment, Source, Campaign, Reference Code and active status.</p></div><div><button class="tp-btn primary" id="tpOpenLinkManager">Open Full Link Manager</button></div></div><div class="tp-tablewrap"><table class="tp-table"><thead><tr><th>Link</th><th>Team Member</th><th>Source</th><th>Campaign</th><th>Reference Code</th><th>Destination</th><th>Status</th><th>Action</th></tr></thead><tbody>${rows.map(l=>{const tm=overview.find(r=>String(r.team_member_id)===String(l.assigned_team_member_id));return `<tr><td><div class="tp-name">${esc(l.name)}</div></td><td>${esc(tm?.display_name||'Unmatched')}</td><td>${esc(l.source||'Other')}</td><td>${esc(l.campaign||'—')}</td><td><code>${esc(l.slug||'—')}</code></td><td>${esc(l.destination_path||'/')}</td><td><span class="tp-badge ${l.is_active?'auto':''}">${l.is_active?'Active':'Disabled'}</span></td><td><button class="tp-btn" onclick="window.TPV206.editLink('${esc(l.id)}')">Edit</button></td></tr>`}).join('')||`<tr><td colspan="8" class="tp-empty">No assigned links.</td></tr>`}</tbody></table></div></div>`;$('tpOpenLinkManager').onclick=()=>{const el=document.querySelector('[data-page="linkmanager"]');if(window.showPage)window.showPage('linkmanager',el);};}
function editLink(id){const l=links.find(x=>String(x.id)===String(id));if(!l)return;$('tpLinkModalBody').innerHTML=`<div class="tp-notice">Changing a reference code changes the shareable referral URL. Existing historical event/link IDs remain preserved.</div><div class="tp-editor" style="margin-top:12px"><div class="wide"><label>Link Name</label><input id="tpLmName" value="${esc(l.name||'')}"></div><div><label>Team Member</label><select id="tpLmTeam"><option value="">No team member</option>${teamOptions(l.assigned_team_member_id)}</select></div><div><label>Source</label><select id="tpLmSource">${['WhatsApp','Facebook','Instagram','YouTube','Email','Google','Other'].map(x=>`<option ${x===l.source?'selected':''}>${x}</option>`).join('')}</select></div><div><label>Campaign</label><input id="tpLmCampaign" value="${esc(l.campaign||'')}"></div><div><label>Reference Code</label><input id="tpLmSlug" value="${esc(l.slug||'')}"></div><div><label>Status</label><select id="tpLmStatus"><option value="1" ${l.is_active?'selected':''}>Active</option><option value="0" ${!l.is_active?'selected':''}>Disabled</option></select></div><div class="wide" style="display:flex;justify-content:flex-end"><button class="tp-btn primary" id="tpLmSave">Save Link</button></div></div>`;$('tpLinkModal').classList.add('open');$('tpLmSave').onclick=()=>saveLink(id);}
async function saveLink(id){try{const c=await waitSb();const slug=$('tpLmSlug').value.trim().toLowerCase().replace(/[^a-z0-9_-]+/g,'-').replace(/^-+|-+$/g,'');if(!slug)throw new Error('Reference code is required.');const {error}=await c.from('tracked_links').update({name:$('tpLmName').value.trim()||slug,assigned_team_member_id:$('tpLmTeam').value||null,source:$('tpLmSource').value,campaign:$('tpLmCampaign').value.trim()||null,slug,is_active:$('tpLmStatus').value==='1'}).eq('id',id);if(error)throw error;$('tpLinkModal').classList.remove('open');toast('Tracking link updated.');await load();activeTab='links';renderLinks();}catch(e){toast(e.message||'Unable to update link','error');}}
async function dailyLots(id,name){const r=dailyRows.find(x=>String(x.team_member_id)===String(id)),z=r?.auto||{},man=r?.manual||{},date=$('tpDailyDate')?.value||todayMY();const ws=z.week_start||date;$('tpLotsModalBody').innerHTML=`<div class="tp-notice"><b>${esc(name)}</b> · Week containing ${esc(date)}. Manual daily activity can be corrected here; broker lots are saved once for the whole week.</div><div class="tp-editor" style="margin-top:12px"><div><label>Leads</label><input id="tpDLeads" type="number" min="0" step="1" value="${num(man.leads)}"></div><div><label>Messages Sent</label><input id="tpDLMsg" type="number" min="0" step="1" value="${num(man.messages_sent)}"></div><div><label>Calls Made</label><input id="tpDLCalls" type="number" min="0" step="1" value="${num(man.calls_made)}"></div><div><label>Follow-ups</label><input id="tpDLFollow" type="number" min="0" step="1" value="${num(man.follow_ups)}"></div><div><label>XM Weekly Lots</label><input id="tpDLXm" type="number" min="0" step="0.01" value="${num(z.weekly_xm_lots??z.xm_lots)}"></div><div><label>DPrime Weekly Lots</label><input id="tpDLDp" type="number" min="0" step="0.01" value="${num(z.weekly_dprime_lots??z.dprime_lots)}"></div><div><label>Exness Weekly Lots</label><input id="tpDLEx" type="number" min="0" step="0.01" value="${num(z.weekly_exness_lots??z.exness_lots)}"></div><div class="wide"><label>Team Member Note / Admin Correction</label><textarea id="tpDLNote">${esc(man.note||'')}</textarea></div><div class="wide tp-notice">Weekly lots will be stored against the Monday week-start automatically. Monthly lot earnings add the weekly records together.</div><div class="wide" style="display:flex;justify-content:flex-end"><button class="tp-btn primary" id="tpDLSave">Save Daily + Weekly Lots</button></div></div>`;$('tpLotsModal').classList.add('open');$('tpDLSave').onclick=async()=>{try{const c=await waitSb(),reportDate=$('tpDailyDate').value;const a=await c.rpc('psp_admin_save_team_daily_activity_v206',{p_team_member_id:id,p_report_date:reportDate,p_leads:num($('tpDLeads').value),p_messages_sent:num($('tpDLMsg').value),p_calls_made:num($('tpDLCalls').value),p_follow_ups:num($('tpDLFollow').value),p_note:$('tpDLNote').value.trim()||null});if(a.error)throw a.error;let b=await c.rpc('psp_admin_save_team_weekly_lots_v207',{p_team_member_id:id,p_week_date:reportDate,p_xm_lots:num($('tpDLXm').value),p_dprime_lots:num($('tpDLDp').value),p_exness_lots:num($('tpDLEx').value),p_admin_note:null});if(b.error)throw b.error;$('tpLotsModal').classList.remove('open');toast('Daily activity and weekly lots saved.');loadDaily();}catch(e){toast(e.message||'Unable to save weekly lots','error');}};}
function renderHistory(selected){const id=selected||overview[0]?.team_member_id;$('tpBody').innerHTML=`<div class="tp-panel"><div class="tp-head"><div><h3>Monthly Performance History</h3><p>Historical months remain independent and are never overwritten by the current month.</p></div><div class="tp-filter"><select id="tpHistTeam">${teamOptions(id)}</select></div></div><div id="tpHistBody"><div class="tp-empty">Loading history…</div></div></div>`;$('tpHistTeam').onchange=()=>loadHistory($('tpHistTeam').value);loadHistory(id);}
async function loadHistory(id){if(!id)return;try{const c=await waitSb();let r=await c.rpc('psp_admin_team_history_v207',{p_team_member_id:id,p_months:12});if(r.error){console.warn('V207 history fallback:',r.error);r=await c.rpc('psp_admin_team_history_v206',{p_team_member_id:id,p_months:12});}if(r.error)throw r.error;const a=Array.isArray(r.data)?r.data:[];$('tpHistBody').innerHTML=`<div class="tp-tablewrap"><table class="tp-table"><thead><tr><th>Month</th><th>Registrations</th><th>Clients</th><th>Course Performance</th><th>VIP Clients</th><th>XM Lots</th><th>DPrime</th><th>Exness</th><th>Total Lots</th><th>Course Earnings</th><th>VIP Earnings</th><th>Lot Earnings</th><th>Total Earnings</th></tr></thead><tbody>${a.map(m=>`<tr><td><strong>${esc(String(m.period_month||'').slice(0,7))}</strong></td><td>${fmt(m.registrations)}</td><td>${fmt(m.clients)}</td><td>${money(m.course_performance)}</td><td>${fmt(m.vip_clients)}</td><td>${fmt(m.xm_lots)}</td><td>${fmt(m.dprime_lots)}</td><td>${fmt(m.exness_lots)}</td><td>${fmt(m.overall_lots)}</td><td>${money(m.course_earnings)}</td><td>${money(m.vip_earnings)}</td><td>${money(m.lot_earnings)}</td><td><strong>${money(m.total_earnings)}</strong></td></tr>`).join('')||`<tr><td colspan="13" class="tp-empty">No history.</td></tr>`}</tbody></table></div>`;}catch(e){$('tpHistBody').innerHTML=`<div class="tp-empty">${esc(e.message||'Unable to load history')}</div>`;}}
function renderTab(){if(!overview.length&&activeTab!=='links'){$('tpBody').innerHTML='<div class="tp-panel"><div class="tp-empty">No team members found.</div></div>';return;}if(activeTab==='overview')renderOverview();else if(activeTab==='members')renderMembers();else if(activeTab==='editor')renderEditor();else if(activeTab==='daily')renderDaily();else if(activeTab==='clients')renderAdminClients();else if(activeTab==='links')renderLinks();else renderHistory();}
function hook(){if(typeof window.showPage!=='function'||window.__tpV206ShowHook)return;window.__tpV206ShowHook=true;const old=window.showPage;window.showPage=function(page,el){const r=old.apply(this,arguments);if(page==='teamperformance'){setTimeout(()=>{if($('pageTitle'))$('pageTitle').textContent='Team Performance';if($('pageSubtitle'))$('pageSubtitle').textContent='Earnings, commissions, attribution, broker lots and daily activity';load();},25);}return r;};}
window.TPV206={openClients(id){activeTab='clients';document.querySelectorAll('#tpTabs .tp-tab').forEach(x=>x.classList.toggle('active',x.dataset.tptab==='clients'));renderAdminClients(id);},openEditor(id){activeTab='editor';document.querySelectorAll('#tpTabs .tp-tab').forEach(x=>x.classList.toggle('active',x.dataset.tptab==='editor'));renderEditor(id);},openHistory(id){activeTab='history';document.querySelectorAll('#tpTabs .tp-tab').forEach(x=>x.classList.toggle('active',x.dataset.tptab==='history'));renderHistory(id);},editLink,dailyLots};
function init(){addStyles();addMenuPage();hook();setTimeout(()=>{addMenuPage();hook();},600);}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
