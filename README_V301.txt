PipSePaisa V301 — PSP TEAM Mobile Scroll + App Install Fix

Fixes:
- One-finger vertical scrolling explicitly enabled on mobile.
- Removed restrictive overscroll setting.
- Closed full-screen More/Filter overlays are now completely removed from hit-testing/display until opened.
- Mobile Data/CSV Export option removed to avoid confusion with app download.
- Mobile download/install actions now clearly say “Install PSP TEAM App”.
- PWA native install prompt remains the download/install action; fallback shows browser Add to Home Screen guidance.
- Service worker/cache version bumped to V301.
- Desktop CSV export remains unchanged.
- No SQL required.
